<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Staff Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .staff-card {
            max-width: 600px;
            margin: 40px auto;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .staff-photo {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 15px;
            border: 3px solid #0d6efd;
        }

        .card-title {
            font-weight: 600;
            color: #0d6efd;
        }

        .card-text strong {
            color: #333;
        }
    </style>
</head>

<body>

    <div class="card staff-card p-4">
        <div class="text-center mt-3">
            <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="Staff Photo"
                class="staff-photo img-fluid d-block mx-auto">
        </div>

        <div class="text-center">
            <h4 class="card-title mt-2">
                <%= data.staff_name %>
            </h4>
            <p class="text-muted mb-1">
                <%= data.department_name %>
            </p>
            <p class="text-muted"><i>
                    <%= data.position || "Staff Member" %>
                </i></p>
        </div>

        <hr>

        <div class="card-body">
            <p class="card-text"><strong>Email:</strong>
                <%= data.email %>
            </p>
            <p class="card-text"><strong>Phone:</strong>
                <%= data.phone_number %>
            </p>
            <p class="card-text"><strong>Hire Date:</strong>
                <%= new Date(data.hire_date).toLocaleDateString('en-IN')  %>
            </p>
            <p class="card-text"><strong>Salary:</strong>
                <i class="bi bi-currency-rupee"></i>
                <%= data.salary %>
            </p>
        </div>

        <div class="text-center mt-3">
            <a href="/student" class="btn btn-primary px-4">Back to List</a>
        </div>
    </div>

</body>

</html>